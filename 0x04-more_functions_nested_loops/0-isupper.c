#include "main.h"

/**
 * _isupper(int c) - check for uppercase character
 * Return: 1
 */
int _isupper(int c);
{
	int c;

	for (c = 'A' && c = 'Z')
	printf("%c: %d\n", c, _isupper(c));
	
	return (1);
}
