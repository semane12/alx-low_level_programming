#include "main.h"

/**
 * _isupper - check for uppercase character
 * @c: variable text
 * Return: 1
 */
int _isupper(int c)
{
	if (c >= 'A' && c = 'Z')
	{
		return (1);
	}
	else
	{
		return (0);
	}
}
